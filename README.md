Deep Learning Course project implementing monocular depth estimation using a deep neural network

The model uses the DPT model as a baseline and is interested in exploring the ability for a network to learn pixel masks with a trained UNet.